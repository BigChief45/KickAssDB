CREATE TABLE Person (
person_id int PRIMARY KEY,
name varchar(20),
gender varchar(1)
);

create table department (
department_name varchar(20),
type varchar(1),
num_employees int,
code varchar(10),
city varchar(15)
);

Create tAbLe pRoduct
(
Product_id int PRImaRY KEY,product_name varchar(26));

INSERT INTO Person
VALUES (154, 'John', 'M');

Insert into person values (155, 'Mary', "G");

